[project]
name = "techpubsai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "techpubs-core",
]

[tool.uv.sources]
techpubs-core = { workspace = true }
